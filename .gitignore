# Python / environment / editors
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
env/
ENV/
.ipynb_checkpoints/
.vscode/
*.code-workspace

# Logs / caches
*.log
*.cache
*.bak
*.tmp
*.swp

# Data files (do NOT add large data files to git)
data/
data/**
*.csv
*.zip
*.tsv
*.json
*.parquet

# Git LFS for model files in /models
# keep models directory in repo but track .pkl via LFS
!models/
# Allow LFS-managed model files to be added
!models/*.pkl
!models/*.joblib

# But ignore any other pickles anywhere else
*.pkl
*.joblib
*.h5

# Env / secrets
.env
.env.*

# Build / dist
build/
dist/
*.egg-info/
